/**
 * Better Auth server instance — same role as `auth.ts` in the
 * [installation guide](https://www.better-auth.com/docs/installation).
 * Implementation lives in `./auth/index.ts` (Drizzle + plugins).
 */
export { auth } from "./auth/index";
